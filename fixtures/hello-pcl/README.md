# Hello PCL fixture

End-to-end fixture for Community Edition M1, built with Public PNPSDK 0.2.5:

```bash
dotnet restore
dotnet build -c Release
```

Produces a development-signed `bin/Release/net10.0/com.pcln.hello-pcl-0.1.0.pnp`.
